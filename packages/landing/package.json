{
  "name": "@spool/landing",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3001",
    "build": "cp -r public dist",
    "deploy": "wrangler pages deploy dist --project-name spool-landing",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
